# Pixso高保真原型搭建执行方案

可以，**当前阶段建议从“生成图 → Pixso高保真可交互原型”推进**。我不直接操作你的 Pixso 文件，但可以输出一套可执行的搭建脚本，你按步骤复制到 Pixso 即可快速完成。

下面我按真实项目交付级流程来做。

---

# Pixso 高保真原型搭建执行方案

## 1\. 目标

基于上一轮生成的 6 张 UI 效果图，在 Pixso 中完成：

1. **完整页面高保真设计稿**

2. **统一设计系统**

3. **可复用组件库**

4. **页面之间跳转原型**

5. **核心交互动效**

6. **前端开发标注规范**

---

# 2\. 搭建前准备

## 2\.1 创建 Pixso 项目

项目名称建议：

```Plaintext
InDigiTwin-AI 工业数字孪生平台
```

项目分类：

```Plaintext
Web端产品设计 / 高保真原型
```

画布基准尺寸：

```Plaintext
1920 × 1080
```

页面比例：

```Plaintext
16:9
```

---

## 2\.2 导入参考图

在 Pixso 中新建一个页面，命名为：

```Plaintext
00_参考图集合
```

把这 6 张生成图分别拖入：

|页面|用途|
|---|---|
|3D 数字孪生主场景首页|核心主界面参考|
|AI 缺陷检测页面|AI功能页参考|
|数据告警列表页面|告警管理页参考|
|场景工程配置页面|模型配置页参考|
|系统权限管理页面|权限管理页参考|
|数据报表统计页面|数据驾驶舱参考|

每张图下方标注页面名称，方便后续搭建时快速对照。

---

# 3\. 第一步：创建设计系统

## 3\.1 颜色样式

进入 Pixso 右侧面板 → 样式 → 新建颜色样式。

建议直接导入这组颜色：

|样式名称|色值|用途|
|---|---|---|
|主色/工业深蓝|`#0F2447`|导航、主背景、品牌色|
|主色/浅1悬浮面板|`#1A3466`|卡片悬浮、菜单选中|
|主色/浅2按钮常态|`#254888`|主按钮、强调按钮|
|状态/紧急告警红|`#E53935`|一级告警、故障高亮|
|状态/正常绿|`#00C853`|设备正常、在线状态|
|状态/预警黄|`#FFB300`|预警、临界状态|
|状态/提示蓝|`#2196F3`|提示、信息链接|
|中性/深色画布|`#0A1830`|3D画布、深色背景|
|中性/卡片底色|`#142A50`|信息卡片、面板|
|中性/分割线|`#2D4066`|分割线、边框|
|中性/辅助文字|`#94A3B8`|次要文字|
|中性/主文字纯白|`#FFFFFF`|标题、正文主文字|
|状态/已处理灰|`#64748B`|已处理、禁用状态|

---

## 3\.2 文字样式

新建文字样式：

|样式名称|字号|字重|行高|
|---|---|---|---|
|文本/页面大标题|24px|600|32px|
|文本/模块标题|18px|500|26px|
|文本/正文常规|14px|400|22px|
|文本/辅助小字|12px|400|18px|
|文本/高亮重点文字|14px|600|22px|

字体建议：

```Plaintext
Inter, 思源黑体, Microsoft YaHei
```

---

## 3\.3 圆角样式

新建圆角样式：

|样式名称|圆角值|
|---|---|
|圆角/按钮常规|4px|
|圆角/卡片|6px|
|圆角/弹窗|8px|

---

## 3\.4 间距样式

新建间距样式：

|样式名称|值|
|---|---|
|间距/极小8|8px|
|间距/常规16|16px|
|间距/弹窗内边距24|24px|

---

## 3\.5 动效样式

新建动效：

|动效名称|时长|缓动|
|---|---|---|
|动效/页面切换淡入|300ms|Linear|
|动效/弹窗弹出|250ms|Ease Out|
|动效/3D镜头过渡|400ms|Ease In Out|
|动效/按钮hover过渡|200ms|Linear|
|动效/面板折叠|300ms|Ease In Out|
|动效/设备闪烁周期|1000ms|Ease In Out|

---

# 4\. 第二步：搭建公共组件库

## 4\.1 组件库页面结构

新建页面：

```Plaintext
01_组件库
```

在该页面下创建组件分类：

```Plaintext
Button 按钮
Tag 标签
Input 输入框
Select 选择器
Table 表格
Card 卡片
Modal 弹窗
Sidebar 侧边栏
Header 顶部导航
StatusBar 底部状态栏
DataChart 数据图表
```

---

## 4\.2 按钮组件

### 主按钮

尺寸：

```Plaintext
120 × 36px
```

背景：

```Plaintext
#254888
```

文字：

```Plaintext
FFFFFF
```

圆角：

```Plaintext
4px
```

文字：

```Plaintext
14px / 600
```

状态：

|状态|样式|
|---|---|
|常态|主色按钮背景|
|Hover|亮度提升 10%|
|点击|透明度 85%|
|禁用|透明度 50%|

---

## 4\.3 危险按钮

尺寸：

```Plaintext
120 × 36px
```

背景：

```Plaintext
#E53935
```

文字：

```Plaintext
FFFFFF
```

圆角：

```Plaintext
4px
```

用途：

```Plaintext
删除、紧急告警、危险操作
```

---

## 4\.4 标签组件

|标签类型|色值|用途|
|---|---|---|
|紧急红|`#E53935`|一级告警|
|预警黄|`#FFB300`|二级预警|
|提示蓝|`#2196F3`|普通提示|
|成功绿|`#00C853`|正常、已恢复|
|已处理灰|`#64748B`|已处理、历史状态|

标签高度：

```Plaintext
24px
```

左右内边距：

```Plaintext
8px
```

文字：

```Plaintext
12px / 400
```

---

## 4\.5 卡片组件

卡片背景：

```Plaintext
#142A50
```

圆角：

```Plaintext
6px
```

内边距：

```Plaintext
16px
```

阴影：

```Plaintext
Y 4px / 模糊 12px / 颜色 #000000 / 透明度 15%
```

标题文字：

```Plaintext
14px / 600 / #FFFFFF
```

内容文字：

```Plaintext
14px / 400 / #94A3B8
```

---

## 4\.6 输入框组件

输入框高度：

```Plaintext
32px
```

背景：

```Plaintext
#0F2447
```

边框：

```Plaintext
#2D4066
```

圆角：

```Plaintext
4px
```

文字：

```Plaintext
14px / #FFFFFF
```

占位符：

```Plaintext
14px / #94A3B8
```

Hover边框：

```Plaintext
#2196F3
```

---

## 4\.7 表格组件

表格背景：

```Plaintext
#142A50
```

表头背景：

```Plaintext
#1A3466
```

分割线：

```Plaintext
#2D4066
```

文字：

```Plaintext
14px / #FFFFFF
```

次要文字：

```Plaintext
12px / #94A3B8
```

表格行高：

```Plaintext
48px
```

Hover行背景：

```Plaintext
#1A3466
```

---

# 5\. 第三步：全局布局组件搭建

## 5\.1 顶部导航栏

高度：

```Plaintext
60px
```

背景：

```Plaintext
#0F2447
```

底部分割线：

```Plaintext
#2D4066
```

左侧内容：

```Plaintext
Logo + InDigiTwin-AI 工业数字孪生平台
```

右侧内容：

```Plaintext
工程名称 / 告警总数 / 用户头像 / 退出
```

文字层级：

```Plaintext
页面大标题：24px
导航文字：14px
辅助信息：12px
```

---

## 5\.2 左侧侧边栏

展开宽度：

```Plaintext
220px
```

折叠宽度：

```Plaintext
60px
```

背景：

```Plaintext
#0F2447
```

菜单项高度：

```Plaintext
48px
```

菜单文字：

```Plaintext
14px / #FFFFFF
```

选中态：

```Plaintext
背景 #1A3466
左侧竖线 #2196F3
文字 #FFFFFF
```

菜单结构：

```Plaintext
场景管理
设备资源库
AI智能检测
数据告警
报表中心
系统设置
```

---

## 5\.3 右侧详情面板

宽度：

```Plaintext
320px
```

背景：

```Plaintext
#142A50
```

内边距：

```Plaintext
16px
```

标题：

```Plaintext
18px / 600 / #FFFFFF
```

模块间距：

```Plaintext
16px
```

可拖拽宽度：

```Plaintext
支持 280px - 400px
```

---

## 5\.4 底部状态栏

高度：

```Plaintext
32px
```

背景：

```Plaintext
#0F2447
```

文字：

```Plaintext
12px / #94A3B8
```

左侧信息：

```Plaintext
场景加载状态 / 模型面片统计 / 在线设备数
```

右侧信息：

```Plaintext
FPS / MQTT状态 / 系统版本
```

---

# 6\. 第四步：逐页高保真搭建

## 6\.1 页面命名规范

建议 Pixso 页面命名：

```Plaintext
02_首页_3D数字孪生主场景
03_AI缺陷检测页
04_数据告警列表页
05_场景工程配置页
06_系统权限管理页
07_数据报表统计页
```

---

## 6\.2 首页：3D数字孪生主场景

页面结构：

```Plaintext
顶部导航 60px
左侧侧边栏 220px
中间3D画布 自适应
右侧详情面板 320px
底部状态栏 32px
```

中间画布背景：

```Plaintext
#0A1830
```

3D区域内容：

```Plaintext
工业设备模型
设备测点高亮
告警闪烁点
框选状态
剖切平面
```

右侧面板模块：

```Plaintext
设备基础信息
实时测点图表
AI分析结果
历史告警
```

左上角悬浮按钮：

```Plaintext
爆炸拆解
模型剖切
视角保存
自动漫游
重置视角
```

---

## 6\.3 AI缺陷检测页

页面结构：

```Plaintext
左侧素材预览区 45%
中间操作区 10%
右侧结果区 45%
```

左侧内容：

```Plaintext
图片预览
摄像头画面
上传按钮
开启摄像头按钮
```

中间内容：

```Plaintext
开始AI检测
清空素材
生成报告
推理模式切换
```

右侧内容：

```Plaintext
缺陷标注图
缺陷类型列表
置信度评分
缺陷统计表格
```

缺陷标注框：

```Plaintext
红色描边 2px
标签背景 #E53935
文字 #FFFFFF
```

---

## 6\.4 数据告警列表页

页面结构：

```Plaintext
顶部筛选区
中间表格列表
右侧统计卡片
```

顶部筛选：

```Plaintext
告警等级
设备类型
时间范围
搜索设备
导出报表
```

表格列：

```Plaintext
告警时间
设备名称
等级
告警内容
状态
操作
```

操作按钮：

```Plaintext
定位
查看详情
标记已处理
删除
```

---

## 6\.5 场景工程配置页

页面结构：

```Plaintext
左侧模型上传区
右侧配置表单区
底部工程列表
```

上传区域：

```Plaintext
拖拽上传GLB/glTF
选择文件按钮
上传进度条
```

配置字段：

```Plaintext
模型名称
LOD等级
Draco压缩
自动减面
碰撞体生成
场景保存
```

工程列表字段：

```Plaintext
工程名称
预览图
模型大小
更新时间
操作
```

---

## 6\.6 系统权限管理页

页面结构：

```Plaintext
左侧角色选择
右侧权限配置
```

角色列表：

```Plaintext
管理员
工程师
巡检员
```

权限分组：

```Plaintext
3D场景权限
AI推理权限
数据导出权限
告警查看权限
系统配置权限
```

组件：

```Plaintext
权限开关
权限说明文字
保存按钮
重置按钮
```

---

## 6\.7 数据报表统计页

页面结构：

```Plaintext
顶部统计卡片
中部趋势图
右侧健康率环形图
底部缺陷统计柱状图
```

统计卡片：

```Plaintext
设备总数
在线率
今日告警
AI检测次数
```

图表内容：

```Plaintext
设备健康趋势
温度压力转速曲线
缺陷类型统计
告警数量趋势
```

---

# 7\. 第五步：组件化处理

## 7\.1 创建主组件

每个页面的全局布局建议做成组件：

```Plaintext
Layout / MainLayout
Header / NavBar
Sidebar / LeftMenu
Panel / RightDetailPanel
StatusBar / Footer
```

页面内业务组件：

```Plaintext
DeviceInfoCard
AlarmList
AiResultPanel
ChartPanel
ModelUploadCard
PermissionSwitchGroup
```

---

## 7\.2 组件命名规范

建议统一命名：

```Plaintext
Category / Name / State
```

示例：

```Plaintext
Button / Primary / Default
Button / Primary / Hover
Tag / Alarm / Red
Card / DataPanel
Input / Normal
```

---

# 8\. 第六步：交互原型设置

## 8\.1 原型页面流程

在 Pixso 原型模式中设置：

```Plaintext
首页
AI缺陷检测页
数据告警列表页
场景工程配置页
系统权限管理页
数据报表统计页
```

---

## 8\.2 侧边栏跳转

交互设置：

```Plaintext
点击“场景管理” → 跳转到首页
点击“AI智能检测” → 跳转到AI缺陷检测页
点击“数据告警” → 跳转到数据告警列表页
点击“报表中心” → 跳转到数据报表统计页
点击“系统设置” → 跳转到系统权限管理页
```

动效：

```Plaintext
淡入淡出
300ms
```

---

## 8\.3 告警列表定位交互

设置：

```Plaintext
点击告警条目
跳转到首页
定位到设备详情
右侧面板展开
设备高亮闪烁
```

动效：

```Plaintext
3D镜头过渡
400ms
Ease In Out
```

---

## 8\.4 AI检测交互

设置：

```Plaintext
点击“开始AI检测”
显示Loading状态
2秒后展示检测结果
右侧结果面板刷新
缺陷框显示在图片上
```

---

## 8\.5 模型上传交互

设置：

```Plaintext
点击“选择文件”
弹出上传弹窗
显示上传进度
上传完成后显示模型缩略图
点击“保存场景”保存工程
```

---

## 8\.6 面板折叠交互

设置：

```Plaintext
点击右侧面板收起按钮
面板宽度从320px变为0px
中间3D画布宽度自动扩展
动效300ms
```

---

# 9\. 第七步：标注与交付

## 9\.1 开发标注内容

每个页面需要标注：

```Plaintext
整体布局尺寸
边距
间距
颜色
字体
圆角
阴影
按钮尺寸
表格行高
弹窗尺寸
动效时长
```

---

## 9\.2 前端交付清单

交付给前端时至少包含：

|交付项|说明|
|---|---|
|页面设计稿|6个核心页面|
|设计系统|颜色、字体、圆角、间距|
|组件库|可复用UI组件|
|原型链接|可点击跳转|
|交互说明|点击、跳转、动效|
|切图资源|图标、图片、模型占位图|
|开发标注|尺寸、色值、字体|

---

# 10\. 推荐的 Pixso 页面结构

你可以直接按这个结构建：

```Plaintext
InDigiTwin-AI 工业数字孪生平台
├── 00_参考图集合
├── 01_设计系统
│   ├── 颜色样式
│   ├── 文字样式
│   ├── 圆角样式
│   ├── 间距样式
│   └── 动效样式
├── 02_组件库
│   ├── 基础组件
│   ├── 业务组件
│   └── 布局组件
├── 03_页面设计稿
│   ├── 首页_3D数字孪生主场景
│   ├── AI缺陷检测页
│   ├── 数据告警列表页
│   ├── 场景工程配置页
│   ├── 系统权限管理页
│   └── 数据报表统计页
├── 04_原型交互
│   ├── 主流程
│   ├── AI检测流程
│   ├── 告警处理流程
│   └── 工程配置流程
└── 05_开发标注
    ├── 全局规范
    ├── 页面标注
    └── 组件标注
```

---

# 11\. 搭建顺序建议

为了避免返工，建议按这个顺序执行：

```Plaintext
第一步：创建项目和画布
第二步：导入参考图
第三步：建立颜色、字体、圆角、间距样式
第四步：搭建按钮、标签、输入框、卡片、表格
第五步：搭建顶部导航、侧边栏、右侧面板、底部状态栏
第六步：逐页搭建6个核心页面
第七步：组件化重构
第八步：设置页面跳转
第九步：设置交互动效
第十步：添加开发标注
第十一步：输出原型链接和交付文档
```

---

# 12\. 当前可交付结论

目前已经完成：

```Plaintext
项目需求框架
技术方案
Demo代码方向
Pixso原型文字规格
Pixso样式JSON
AI绘图完整Prompt
整套UI效果图
```

下一步进入 Pixso 后，核心工作就是：

```Plaintext
建立设计系统 → 搭建组件库 → 搭建页面 → 设置交互 → 标注交付
```

这套项目已经具备从**产品需求 → UI设计 → 原型交互 → 前端开发**的完整基础。

> (Note: May contain AI-generated content.)
