# Pixso 自动化高保真原型搭建操作清单（可直接落地执行）

**适用项目**：InDigiTwin\-AI 工业数字孪生平台

**适用流程**：AI效果图参考 \+ 一键导入样式 \+ 自动化组件库 \+ 自动布局 \+ 批量交互原型 \+ 自动标注交付

**目标**：零硬编码样式、全局统一、可批量复用、可直接交付前端

---

## 一、前期准备（必须全部就位）

将以下物料提前准备好，统一放入项目文件夹：

- ✅ 6 张 AI 生成整套 UI 效果图（页面底图参考）

- ✅ Pixso 全局样式 JSON 文件（颜色/文字/圆角/间距/动效）

- ✅ 项目 UI 规格文档（色值、字体、布局、组件尺寸）

- ✅ 页面结构清单 \+ 交互流程文档

---

## 二、第一步：新建 Pixso 项目 \& 基础画布配置（自动化初始化）

### 2\.1 项目配置

- 项目名称：`InDigiTwin-AI 工业数字孪生平台`

- 设计类型：Web 端高保真原型

- 基准画布尺寸：**1920 × 1080**

- 画布比例：16:9 工业大屏标准

### 2\.2 初始化页面目录

新建 6 个顶层页面文件夹：

- `00_参考图集合`

- `01_设计系统`

- `02_组件库`

- `03_页面设计稿`

- `04_原型交互流程`

- `05_开发标注交付`

### 2\.3 导入参考底图（全自动视觉对位）

进入 `00_参考图集合`，导入全部 6 张 UI 效果图，作为底层固定参考图。

**作用**：所有页面直接对位 AI 效果图，保证视觉 1:1 还原，无偏差。

---

## 三、第二步：一键导入样式系统（全自动统一规范）

进入「右侧样式面板 → 更多 → 导入样式」

粘贴项目专属 **Pixso 样式 JSON**，自动生成：

- 全部工业色板（主色/状态色/中性色）

- 全部 5 套文字层级样式

- 圆角规范（4px / 6px / 8px）

- 间距规范（8 / 16 / 24）

- 全套动效时长与缓动

> **强制规则**：后续所有 UI 元素**禁止手写色值、字号、圆角**，必须绑定全局样式。
> 
> 

---

## 四、第三步：自动化搭建组件库（自动布局 \+ 主组件复用）

进入 `02_组件库`，分三类搭建：基础组件、业务组件、布局组件。

### 4\.1 基础组件（全部开启自动布局）

批量制作并保存为「主组件」：

- 按钮：主按钮、次要按钮、危险按钮（4 态：默认/hover/点击/禁用）

- 状态标签：红/黄/蓝/绿/灰 5 种告警标签

- 输入框、下拉选择器

- 数据卡片、弹窗、提示框

- 表格组件（固定行高 48px、hover 变色）

### 4\.2 全局布局组件（页面壳子全自动复用）

制作全局 Layout 主组件：

- 顶部导航栏（高 60px）

- 左侧侧边栏（220px / 60px 折叠）

- 右侧详情面板（320px）

- 底部状态栏（32px）

**效果**：所有页面直接拖拽 Layout 实例，无需重复画布局。

### 4\.3 业务组件

- 设备信息卡片

- AI 缺陷结果面板

- 告警列表项

- 数据图表面板

- 模型上传组件

- 权限开关组件

---

## 五、第四步：6 大核心页面半自动高保真搭建

进入 `03_页面设计稿`，新建 6 个标准页面：

1. 02\_首页\_3D数字孪生主场景

2. 03\_AI缺陷检测页

3. 04\_数据告警列表页

4. 05\_场景工程配置页

5. 06\_系统权限管理页

6. 07\_数据报表统计页

### 页面统一搭建流程（标准化）

- 底层放入对应 AI 参考效果图

- 拖拽全局 Layout 组件搭好外壳

- 拖拽业务组件对位参考图

- 3D 画布区域填充深色画布底色 \#0A1830

- 所有样式绑定全局样式库

---

## 六、第五步：批量原型交互自动化配置（可直接演示）

切换「原型模式」，批量配置全站跳转与动效，全部使用全局动效样式。

### 6\.1 全局菜单跳转（统一 300ms 淡入淡出）

- 场景管理 → 首页

- AI 智能检测 → AI 缺陷检测页

- 数据告警 → 告警列表页

- 报表中心 → 数据报表页

- 系统设置 → 权限管理页

### 6\.2 核心业务交互

- 告警列表点击条目 → 跳转首页 \+ 展开右侧面板 \+ 设备闪烁动效

- AI 检测按钮 → Loading 状态 → 2s 后展示缺陷结果

- 右侧面板折叠/展开（300ms 弹性过渡）

- 模型上传弹窗弹出（250ms 弹出动效）

---

## 七、第六步：自动标注 \+ 全套交付物输出

进入「开发模式」→ 开启自动标注

输出全套交付物：

- 完整 Pixso 源文件

- 可在线演示交互原型链接

- 全局设计系统导出文件

- 组件库说明

- 页面自动标注尺寸/色值/字体/间距

- 批量切图资源

- 交互说明文档

---

## 八、最终验收校验清单（必过）

- \[ \] 所有样式均为全局样式引用，无硬编码

- \[ \] 所有 UI 均为组件实例，支持全局批量更新

- \[ \] 6 个页面结构完全匹配项目规范

- \[ \] 所有跳转、弹窗、状态、动效全部正常可演示

- \[ \] 自动标注完整、可直接交付前端开发

- \[ \] 视觉与 AI 效果图 1:1 高保真一致

---

## 九、最终成型工作流总结

**AI效果图参考 → JSON一键样式导入 → 自动化组件库 → 自动布局页面搭建 → 批量原型交互 → 自动标注交付**

实现：**标准化、高统一、低返工、可工程化、可直接对接 Three\.js 前端项目**

> (Note: May contain AI-generated content.)
